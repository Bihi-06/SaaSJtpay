import React from "react";

export function Toaster() {
  // Simplified version that doesn't actually show toasts
  // We're keeping this to avoid breaking the existing app structure
  return null;
}
